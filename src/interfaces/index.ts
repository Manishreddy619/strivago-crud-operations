export interface Users {
  email: string;
  password: string;
  role: string;
  refreshToken: string;
  google_id: string;
}
export interface Accommodation {
  name: string;
  description: string;
  city: string;
  totalGuest: string;
  userId: string;
}
